module.exports = {
    // root: true,
    env: {
        browser: true,
        node: true,
        es6: true,
    },

    extends: ['universe'],

    parserOptions: { ecmaVersion: 8 }, // to enable features such as async/await
};
